## Checklist

- [ ] `./scripts/validate.sh` passes locally (SwiftPM + coverage gate + XcodeGen drift check + `xcodebuild` test).
- [ ] If you changed `project.yml` or any path under `Tests/`, ran `xcodegen generate` at the repo root and committed `iOSArchitectureShowcase.xcodeproj/project.pbxproj`.

## Notes (optional)

